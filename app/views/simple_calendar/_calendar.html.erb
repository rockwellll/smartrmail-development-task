<div class="w-full">
<!--  <div class="calendar-heading">-->
    <%#= link_to t('simple_calendar.previous', default: 'Previous'), calendar.url_for_previous_view %>
<!--    <span class="text-red-600"><%#= t('date.month_names')[start_date.month] %> <%#= start_date.year %></span>-->
    <%#= link_to t('simple_calendar.next', default: 'Next'), calendar.url_for_next_view %>
<!--  </div>-->

  <table class="w-full text-center">
<!--    <thead>-->
<!--      <tr>-->
        <%# date_range.slice(0, 7).each do |day| %>
<!--          <th class="text-bold border"><%#= t('date.abbr_day_names')[day.wday] %></th>-->
        <%# end %>
<!--      </tr>-->
<!--    </thead>-->

    <tbody>
      <% date_range.each_slice(7) do |week| %>
        <%= content_tag :tr, class: calendar.tr_classes_for(week) do %>
          <% week.each do |day| %>
            <%= content_tag :td, class: "border p-1" do %>
            <span class="text-bold text-sm mb-3"><%= t('date.day_names')[day.wday] %></span>
              <% if defined?(Haml) && respond_to?(:block_is_haml?) && block_is_haml?(passed_block) %>
                <% capture_haml(day, sorted_events.fetch(day, []), &passed_block) %>
              <% else %>
                <% passed_block.call day, sorted_events.fetch(day, []) %>
              <% end %>
            <% end %>
          <% end %>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
